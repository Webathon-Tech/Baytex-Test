# Terraform working directories and state
.terraform/
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Environment-specific inputs and backend config.
# The .example templates are what ship; the real files never are.
*.tfvars
*.tfvars.json
backend.hcl

# Saved plans and rendered plan output
*.tfplan
tfplan
plan.txt
*-plan.txt

# Generated handoff / validation artefacts
handoff/
outputs.json
*-outputs.json
run-metadata.json
state-before.txt
state-after.txt
state-list.txt
apply.log
plan-checksum.txt
connectivity-test-results.json

# Local-only scaffolding, never part of the deliverable.
sandbox/

# Network diagrams (binary)
*.vsdx
